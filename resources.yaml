---
apiVersion: v1
kind: Service
metadata:
  name: herb-city-user-service
spec:
  type: LoadBalancer
  selector:
    app: herb-city-user-service-image
  ports:
  - protocol: TCP
    port: 5007
    targetPort: 5007
    nodePort: 31110
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: herb-city-user-service-deployment
  labels:
    app: herb-city-user-service-image
spec:
  replicas: 1
  selector:
    matchLabels:
      app: herb-city-user-service-image
  template:
    metadata:
      labels:
        app: herb-city-user-service-image
    spec:
      containers:
      - name: nodeserver
        image: us-west2-docker.pkg.dev/herb-city/herb-city-user-service-repo/herb-city-user-service-image:tag1
        ports:
        - containerPort: 5000